@model IEnumerable<SakilaApp.Models.Category>
@{
    ViewData["Title"] = "Categorías";
    var search     = ViewData["Search"] as string ?? "";
    var page       = (int)(ViewData["Page"] ?? 1);
    var total      = (int)(ViewData["Total"] ?? 0);
    var totalPages = (int)(ViewData["TotalPages"] ?? 1);
}

<div class="page-header">
    <h1><i class="bi bi-tags me-2 text-success"></i>Categorías</h1>
    <a asp-action="Create" class="btn btn-success">
        <i class="bi bi-plus-lg me-1"></i>Nueva categoría
    </a>
</div>

<div class="search-section">
    <form asp-action="Index" method="get">
        <div class="input-group">
            <span class="input-group-text bg-white border-end-0">
                <i class="bi bi-search text-muted"></i>
            </span>
            <input type="text" name="search" value="@search" class="form-control border-start-0"
                   placeholder="Buscar por nombre de categoría..." />
            <button type="submit" class="btn btn-success px-4">Buscar</button>
            @if (!string.IsNullOrWhiteSpace(search))
            {
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-x-lg"></i>
                </a>
            }
        </div>
    </form>
</div>

<div class="stats-bar px-1">
    @if (!string.IsNullOrWhiteSpace(search))
    {
        <span><i class="bi bi-funnel me-1"></i>@total resultado(s) para "<strong>@search</strong>"</span>
    }
    else
    {
        <span><i class="bi bi-tags me-1"></i>@total categoría(s) en total</span>
    }
    &nbsp;&mdash; Página @(page) de @(totalPages)
</div>

<div class="card-table">
    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th style="width:70px">ID</th>
                <th>Nombre</th>
                <th style="width:180px">Última actualización</th>
                <th style="width:200px" class="text-center">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><span class="badge bg-secondary">@item.CategoryId</span></td>
                    <td>
                        <span class="badge bg-success bg-opacity-10 text-success border border-success border-opacity-25 fw-normal py-2 px-3">
                            <i class="bi bi-tag me-1"></i>@item.Name
                        </span>
                    </td>
                    <td class="text-muted small">@item.LastUpdate.ToString("dd/MM/yyyy")</td>
                    <td class="text-center">
                        <a asp-action="Details" asp-route-id="@item.CategoryId" class="btn btn-sm btn-outline-info" title="Ver detalles">
                            <i class="bi bi-eye"></i>
                        </a>
                        <a asp-action="Edit" asp-route-id="@item.CategoryId" class="btn btn-sm btn-outline-warning" title="Editar">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.CategoryId" class="btn btn-sm btn-outline-danger" title="Eliminar">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
            }
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="4" class="text-center text-muted py-4">
                        <i class="bi bi-inbox fs-3 d-block mb-2"></i>
                        No se encontraron categorías@(!string.IsNullOrWhiteSpace(search) ? $" para \"{search}\"." : ".")
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (totalPages > 1)
{
    <nav aria-label="Paginación" class="mt-3">
        <ul class="pagination justify-content-center flex-wrap">
            <li class="page-item @(page <= 1 ? "disabled" : "")">
                <a class="page-link" asp-action="Index" asp-route-search="@search" asp-route-page="1">«</a>
            </li>
            <li class="page-item @(page <= 1 ? "disabled" : "")">
                <a class="page-link" asp-action="Index" asp-route-search="@search" asp-route-page="@(page - 1)">‹</a>
            </li>
            @{ int from = Math.Max(1, page - 2); int to = Math.Min(totalPages, page + 2); }
            @if (from > 1) { <li class="page-item disabled"><span class="page-link">…</span></li> }
            @for (int p = from; p <= to; p++)
            {
                <li class="page-item @(p == page ? "active" : "")">
                    <a class="page-link" asp-action="Index" asp-route-search="@search" asp-route-page="@p">@p</a>
                </li>
            }
            @if (to < totalPages) { <li class="page-item disabled"><span class="page-link">…</span></li> }
            <li class="page-item @(page >= totalPages ? "disabled" : "")">
                <a class="page-link" asp-action="Index" asp-route-search="@search" asp-route-page="@(page + 1)">›</a>
            </li>
            <li class="page-item @(page >= totalPages ? "disabled" : "")">
                <a class="page-link" asp-action="Index" asp-route-search="@search" asp-route-page="@totalPages">»</a>
            </li>
        </ul>
    </nav>
}
